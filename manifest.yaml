---
applications:
- name: agent
  no-route: true
  buildpacks:
    - https://github.com/cloudfoundry/apt-buildpack
    - binary_buildpack
  memory: 1G
  disk_quota: 4G
  env:
    ANTHROPIC_API_KEY: ((anthropic_api_key))
    GH_TOKEN: ((github_token))
  health-check-type: none
  processes:
    - type: web
      command: sleep infinity
      instances: 0
    - type: task
      command: ./bin/claude -p "hello" < /dev/null